
package events;


public class DoorLocking {
    private boolean lock;

    public DoorLocking(boolean lock) {
        this.lock = lock;
    }

  
    public boolean isLock() {
        return lock;
    }

    
}
